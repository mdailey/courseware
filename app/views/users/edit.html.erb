
<h2>User profile</h2>

<% @user.old_password = @user.password = @user.password_confirmation = nil %>

<%= error_messages_for :user %>

<% form_for @user do |f| -%>
  <%= render :partial => "user_form_fields", :locals => { :form => f, :submit_text => 'Update', :user => @user } %>
<% end -%>
